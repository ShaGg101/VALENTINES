# Example environment variables for Vite (copy this file to `.env` and fill values)
# IMPORTANT: Variables exposed to client-side must be prefixed with VITE_

# App metadata
VITE_APP_TITLE="Valentines"
VITE_PUBLIC_PATH="/"

# API / Backend
VITE_API_URL="https://api.example.com"

# Feature flags
VITE_FEATURE_HEART_EASTER_EGG=true

# Analytics / monitoring (leave empty if not used)
VITE_ANALYTICS_ID=""
VITE_SENTRY_DSN=""

# Notes:
# - Do not commit secrets or sensitive values to git. Keep .env files out of version control.
# - Access values in client code using import.meta.env.VITE_API_URL
# - For local-only overrides, you can use .env.local (which should be ignored by git)
